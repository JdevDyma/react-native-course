import { StyleSheet } from "react-native";
import { colors } from "../constants/colors";

export const articlesStyles = StyleSheet.create({
  borderTopPage: { borderTopWidth: 1, borderTopColor: colors.primary },
});
